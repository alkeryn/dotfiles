#!/usr/bin/env bash
mkdir -p ~/App/{chroot,ISO,VM,wine}
mkdir -p ~/Cloud/Mega
mkdir ~/Downloads
mkdir -p ~/tmp/tmp

pacman -Syu $(cat /home/alkeryn/bin/etc/Install/pacman)
#mkdir -p ~/{App/{chroot,ISO,VM,wine},Cloud/Mega,Downloads,tmp/tmp}

#~/bin/misc/install/zsh
